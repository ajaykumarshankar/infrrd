import { CloseOutsideClickDirective } from './close-outside-click.directive';

describe('CloseOutsideClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseOutsideClickDirective();
    expect(directive).toBeTruthy();
  });
});
